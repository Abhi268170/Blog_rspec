<%= form_for([@article, @article.comments.build]) do |f| %> 


  <p>
    <%= f.label 'comment' %>
    
     <%= f.text_field :body, required: true %>
  </p>
  <p>
    <%= f.submit 'comment' %>
  </p>
<% end %>